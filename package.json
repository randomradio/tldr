{
  "name": "tldr-extension",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "test": "vitest run",
    "clean": "rm -rf dist",
    "package": "node scripts/package.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.43",
    "esbuild": "^0.21.5",
    "typescript": "^5.5.4",
    "vitest": "^4.1.8"
  }
}
