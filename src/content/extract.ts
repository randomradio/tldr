// Intentionally minimal; extraction is executed via scripting.executeScript from background.
// Keeping this file so it’s bundled and available if we decide to register a persistent content script later.
export {};

